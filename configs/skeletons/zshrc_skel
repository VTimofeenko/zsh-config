# Set variables if paths are different:
# ZSH_ASYNC_PATH
# ZSH_FZF_BINDINGS_PATH
# ZSH_AUTOSUGGESTIONS_PATH
# Set variables to 'n' in order to not load specific plugins:
# _ZSH_VT_GIT_PLUGIN
# _ZSH_VT_JOBS_PLUGIN
# #---Sourcing the base config---#
source ${HOME}/.config/zsh/shared-config/zshrc_base

# Should be sourced last (!)
zsh_highlightning_loc="/usr/share/zsh/site-contrib/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh"
[ -f "$zsh_highlightning_loc" ] && source "$zsh_highlightning_loc"

# Make comments without style
ZSH_HIGHLIGHT_STYLES[comment]='none'
